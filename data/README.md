# Dataset

Place the RadioML HDF5 dataset here before running the notebook:

```text
GOLD_XYZ_OSC.0001_1024.hdf5
```

The dataset is ignored by Git because it is too large for a normal repository commit.
